---
phase: 01-ui-ux-audit-polish
plan: 10
type: execute
wave: 4
depends_on: [01, 02, 03, 04, 05, 06, 07, 08, 09]
files_modified:
  - .planning/phases/01-ui-ux-audit-polish/01-AUDIT.md
  - .planning/phases/01-ui-ux-audit-polish/screenshots/
requirements: [UI-01, UI-02, UI-03]
autonomous: false

must_haves:
  truths:
    - "01-AUDIT.md tracker exists covering all 47 routes × applicable states"
    - "All 6 UI-SPEC §14 quality gates run as automated greps + builds, producing pass/fail per gate"
    - "Every row in the tracker is either ✓ Verified YYYY-MM-DD, n/a, or ⚠ Deviation: <reason>"
    - "8 spot screenshots captured per D-13 + UI-SPEC §12.6 list, attached/linked in the tracker"
    - "Phase exit gate (D-14): no row left in ☐ Pending state when this plan completes"
    - "Findings are routed: pass → mark ✓; deviation → document; defect → either fix-in-this-plan (small) or open a gap-closure entry for `/gsd-plan-phase 1 --gaps`"
  artifacts:
    - path: ".planning/phases/01-ui-ux-audit-polish/01-AUDIT.md"
      provides: "Full audit tracker with all 47 routes × applicable states + quality-gate results + screenshot links"
      contains: "Phase 1 — UI/UX Audit Tracker"
    - path: ".planning/phases/01-ui-ux-audit-polish/screenshots/"
      provides: "8 spot screenshots per D-13"
      min_files: 8
  key_links:
    - from: "AUDIT tracker"
      to: "All 47 routes via npm run dev + dev-param URLs"
      via: "Manual browser verification per row"
      pattern: "✓ Verified"
---

<objective>
Final phase gate. Walk every row in `01-AUDIT.md` (47 routes × applicable states from UI-SPEC §12) using the local dev server + dev-param URLs. Flip each cell from ☐ Pending → ✓ Verified / n/a / ⚠ Deviation. Run all 6 UI-SPEC §14 quality gates as automated greps + builds. Capture 8 spot screenshots per D-13. Phase 1 is NOT done until every row is ✓ or explicitly n/a / deviation-documented (D-14).

Purpose: The audit IS the phase exit gate per D-14. Plans 01-09 produced the artifacts; this plan proves they work end-to-end and produces the hand-off artefact (the tracker + screenshots) that next-phase planning can reference.

Output: 1 markdown tracker file + 8 screenshot images + a final pass/fail SUMMARY.

This plan is `autonomous: false` because Task 3 (browser verification + screenshot capture) is a `checkpoint:human-verify` — Claude generates the tracker, runs quality gates autonomously, opens the dev server and visits URLs autonomously, BUT the human is the final signer-off on visual rendering correctness for each route + the source of the 8 screenshot files.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ui-ux-audit-polish/01-CONTEXT.md
@.planning/phases/01-ui-ux-audit-polish/01-UI-SPEC.md
@.planning/phases/01-ui-ux-audit-polish/01-PATTERNS.md
@.planning/phases/01-ui-ux-audit-polish/01-01-SUMMARY.md
@.planning/phases/01-ui-ux-audit-polish/01-05-SUMMARY.md
@.planning/phases/01-ui-ux-audit-polish/01-06-SUMMARY.md
@.planning/phases/01-ui-ux-audit-polish/01-07-SUMMARY.md
@.planning/phases/01-ui-ux-audit-polish/01-08-SUMMARY.md
@.planning/phases/01-ui-ux-audit-polish/01-09-SUMMARY.md

<interfaces>
<!-- The 6 UI-SPEC §14 quality gates -->

1. **Density mirrors TOS** — manual side-by-side at 1440×900 (`/dashboard` shows 4 KPI tiles + first chart above the fold)
2. **No big page-body H1** — `grep -r "<h1" src/app/` returns ONLY hits in /, /login, /forgot-password
3. **Token discipline** — no new hex literals outside overlay §6.4; every value resolves to var(--gecko-*) or matches existing TOS literals (32/30/40/42/15/11)
4. **State coverage** — every list route in §12.1 has 4 states; every detail route in §12.2 has 2 states
5. **Copy realism** — no `Customer A` / `Container 12345` / `Lorem` / `$100` / `Sample Co` in src/ outside /test/; BIC check digits all compute correctly
6. **Brand string** — `grep -ri "logicon" src/` returns zero hits outside .gitignore and comments-of-record

<!-- The 8 spot screenshots per D-13 + UI-SPEC §12.6 -->
1. `/dashboard` — KPI tile density (1440×900 viewport)
2. `/equipment` — list table density
3. `/equipment?type=REEFER&empty=1` — filter-empty render
4. `/repair/REP-DOES-NOT-EXIST` — not-found render with Levenshtein suggestion
5. `/survey/[id-of-any-real-survey]?loading=1` — detail spinner
6. `/cleaning?error=1` — error state with disclosure (expand the <details>)
7. `/billing` — table card density
8. `/tariff/contracts/[id-of-any-real-contract]` — detail page header (verifies no big H1)

<!-- The 47-route inventory from UI-SPEC §12 -->
- List routes (28): see UI-SPEC §12.1
- Detail routes (10): see §12.2
- Form/create routes (11): see §12.3 (only Loading + Error on submit are in scope)
- Public routes (3): /, /login, /forgot-password (narrower bar per D-15)

<!-- AUDIT cell vocabulary (UI-SPEC §12.5) -->
- ☐ Pending — not yet verified
- ✓ Verified YYYY-MM-DD — visually confirmed in browser
- n/a — state doesn't apply
- ⚠ Deviation: <reason> — implemented but with a documented deviation
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| dev server runs locally | `npm run dev` on localhost. No production exposure during audit. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-10-01 | Spoofing (false ✓) | Audit tracker checkmarks | mitigate | Tracker is human-verified per row at checkpoint task 3. Each ✓ is initialled with a date — if a row is flipped without verification, the next phase's planning will catch the mismatch when it tries to consume the artifact. |
| T-10-02 | Information Disclosure | Screenshots committed to repo | accept | Screenshots are of seed data (no real customer info). Safe to commit. |
| T-10-03 | Tampering (dev-param production leak) | Final production-build check | mitigate | Quality-gate run includes a `NODE_ENV=production npm run build` smoke that confirms dev-params don't activate. Also: `grep -E "sp.get..(loading|error|empty|filter-empty)" src/app/` shows that every hit is co-located with `process.env.NODE_ENV` (per plans 08/09 acceptance). |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Generate the 01-AUDIT.md tracker scaffold (all 47 rows × applicable states, all ☐ Pending)</name>

  <read_first>
    - d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api/.planning/phases/01-ui-ux-audit-polish/01-UI-SPEC.md §12 (all 47 routes — the authoritative inventory)
    - d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api/.planning/phases/01-ui-ux-audit-polish/01-CONTEXT.md D-12, D-13, D-14 (tracker shape + sign-off bar)
    - d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api/.planning/phases/01-ui-ux-audit-polish/01-PATTERNS.md §14 (lines 803-852 — recommended layout)
    - Run: `find src/app -name "page.tsx" | sort` to confirm the current 47-page inventory matches UI-SPEC §12 (re-counting protects against route additions/removals during the phase)
  </read_first>

  <files>.planning/phases/01-ui-ux-audit-polish/01-AUDIT.md</files>

  <action>
**Step A: Run `find src/app -name "page.tsx" | sort`** to get the authoritative current inventory. If the count differs from UI-SPEC §12's 47, document the delta at the top of the tracker — extra routes get added as rows; deleted routes don't appear.

**Step B: Create `.planning/phases/01-ui-ux-audit-polish/01-AUDIT.md`** with the structure below. Use the EXACT cell vocabulary from UI-SPEC §12.5.

```markdown
---
phase: 01-ui-ux-audit-polish
type: audit
status: in_progress  # → 'complete' when last row flips ✓ / n/a / deviation
generated: 2026-05-18
last_updated: 2026-05-18
---

# Phase 1 — UI/UX Audit Tracker

**Authoritative inventory:** `find src/app -name "page.tsx" | sort` returned N pages (UI-SPEC §12 expected: 47). Delta: <none | + extras | - deletions documented>.

**Cell vocabulary (UI-SPEC §12.5):**
- ☐ Pending — not yet verified
- ✓ YYYY-MM-DD — visually confirmed in browser at `npm run dev`
- n/a — state doesn't apply to this route
- ⚠ Deviation: <reason> — implemented but with a documented deviation

**Sign-off bar:** Phase 1 is not "done" until every row is ✓ / n/a / ⚠ (D-14).

---

## A. Quality Gates (UI-SPEC §14 — automated)

| # | Gate | Command | Result |
|---|------|---------|--------|
| 1 | Density mirrors TOS | Manual side-by-side at 1440×900 | ☐ Pending |
| 2 | No big page-body H1 | `grep -rln "<h1" src/app/ \| grep -vE "(page.tsx\|login\|forgot-password)"` returns 0 | ☐ Pending |
| 3 | Token discipline | Manual grep of overlay §6.4 for `#[0-9a-f]{3,6}` (zero outside the §6.4 block) | ☐ Pending |
| 4 | State coverage | Every row in §B and §C below is ✓ | ☐ Pending |
| 5 | Copy realism | `grep -rE "Customer A\b\|Container 12345\b\|\$[0-9]+\b\|Lorem\|Sample Co\b" src/ \| grep -v test` returns 0 AND `npm test -- equipment.validation` passes | ☐ Pending |
| 6 | Brand string | `grep -ri "logicon-mnr" src/` returns 0 | ☐ Pending |

---

## B. List routes (UI-SPEC §12.1 — 28 routes)

Each row verified by visiting the URL at `npm run dev` with the indicated query params.

| # | Route | Empty<br/>`?empty=1` | Filter-Empty<br/>`?filter-empty=1` | Loading<br/>`?loading=1` | Error<br/>`?error=1` | Notes |
|---|-------|:--:|:--:|:--:|:--:|-------|
| 1 | `/dashboard` | ☐ | n/a | ☐ | ☐ | KPI tiles always show — empty only triggers with empty seed |
| 2 | `/equipment` | ☐ | ☐ | ☐ | ☐ | Test filter-empty with `?type=REEFER&filter-empty=1` |
| 3 | `/repair` | ☐ | ☐ | ☐ | ☐ | Kanban — empty replaces whole page |
| 4 | `/survey` | ☐ | ☐ | ☐ | ☐ | |
| 5 | `/cleaning` | ☐ | ☐ | ☐ | ☐ | |
| 6 | `/storage` | ☐ | ☐ | ☐ | ☐ | |
| 7 | `/parts` | ☐ | ☐ | ☐ | ☐ | |
| 8 | `/billing` | ☐ | ☐ | ☐ | ☐ | Filter-empty: `?status=overdue&filter-empty=1` |
| 9 | `/modification` | ☐ | ☐ | ☐ | ☐ | |
| 10 | `/emergency` | ☐ | ☐ | ☐ | ☐ | |
| 11 | `/tariff` | ☐ | n/a | ☐ | ☐ | Hub page |
| 12 | `/tariff/rate-cards` | ☐ | ☐ | ☐ | ☐ | |
| 13 | `/tariff/customer-rates` | ☐ | ☐ | ☐ | ☐ | |
| 14 | `/tariff/contracts` | ☐ | ☐ | ☐ | ☐ | |
| 15 | `/tariff/surcharges` | ☐ | ☐ | ☐ | ☐ | |
| 16 | `/tariff/simulator` | ☐ | n/a | ☐ | ☐ | Empty = "Run a simulation" CTA |
| 17 | `/tariff/history` | ☐ | ☐ | ☐ | ☐ | |
| 18 | `/settings` | n/a | n/a | ☐ | ☐ | Hub |
| 19 | `/settings/profile` | n/a | n/a | ☐ | ☐ | Form — submit-time states only |
| 20 | `/settings/notifications` | n/a | n/a | ☐ | ☐ | Form |
| 21 | `/settings/language` | n/a | n/a | ☐ | ☐ | Form |
| 22 | `/settings/company` | n/a | n/a | ☐ | ☐ | Form |
| 23 | `/settings/display` | n/a | n/a | ☐ | ☐ | Form |
| 24 | `/settings/users` | ☐ | ☐ | ☐ | ☐ | |
| 25 | `/settings/integrations` | ☐ | n/a | ☐ | ☐ | |

## C. Detail routes (UI-SPEC §12.2 — 10 routes + 1 edit variant)

| # | Route | Loading<br/>`?loading=1` | Not-Found<br/>`/<id-that-doesnt-exist>` | Notes |
|---|-------|:--:|:--:|-------|
| 26 | `/equipment/[id]` | ☐ | ☐ | Test: `/equipment/MSKU2345675?loading=1` + `/equipment/DOES-NOT-EXIST` |
| 27 | `/repair/[id]` | ☐ | ☐ | Test: `/repair/REP-2026-0001?loading=1` + `/repair/REP-XYZ`; verify "Did you mean" |
| 28 | `/survey/[id]` | ☐ | ☐ | |
| 29 | `/cleaning/[id]` | ☐ | ☐ | |
| 30 | `/parts/[id]` | ☐ | ☐ | |
| 31 | `/billing/[id]` | ☐ | ☐ | |
| 32 | `/modification/[id]` | ☐ | ☐ | |
| 33 | `/emergency/[id]` | ☐ | ☐ | |
| 34 | `/tariff/contracts/[id]` | ☐ | ☐ | |
| 35 | `/tariff/customer-rates/[customerId]` | ☐ | ☐ | Test both: customer-doesn't-exist (suggestion) + customer-exists-no-overrides |
| 36 | `/tariff/customer-rates/[customerId]/edit` | ☐ | ☐ | |

## D. Form / create routes (UI-SPEC §12.3 — 11 routes — Loading + Error on submit only)

| # | Route | Loading<br/>on submit | Error<br/>on submit | Notes |
|---|-------|:--:|:--:|-------|
| 37 | `/repair/new` | ☐ | ☐ | Existing form — Phase 1 verifies gecko-styled spinner-in-button + gecko-alert-error on failure |
| 38 | `/survey/new` | ☐ | ☐ | |
| 39 | `/cleaning/new` | ☐ | ☐ | |
| 40 | `/parts/new` | ☐ | ☐ | |
| 41 | `/modification/new` | ☐ | ☐ | |
| 42 | `/tariff/rate-cards/new` | ☐ | ☐ | |
| 43 | `/tariff/customer-rates/new` | ☐ | ☐ | |
| 44 | `/tariff/contracts/new` | ☐ | ☐ | |
| 45 | `/tariff/surcharges/new` | ☐ | ☐ | |

> **Form-page bar (narrow per D-15 implicit):** the existing form pages already have client-side submit handlers. Phase 1's bar is "Loading state visible during async submit + gecko-styled inline error on failure." If a form has neither (or uses non-gecko Tailwind chrome), mark ⚠ Deviation and either fix-in-this-plan or open gap-closure.

## E. Public / chromeless (UI-SPEC §12.4 — 3 routes, narrower bar per D-15)

| # | Route | Brand string<br/>`grep -i "logicon"` | Loading | Error | Testimonial (page only) | Notes |
|---|-------|:--:|:--:|:--:|:--:|-------|
| 46 | `/` | ☐ | n/a | n/a | ☐ | Tan Wei Ming quote per UI-SPEC §10.4 |
| 47 | `/login` | ☐ | ☐ | ☐ | n/a | Inline gecko-alert-error on auth fail |
| 48 | `/forgot-password` | ☐ | ☐ | ☐ | n/a | |

## F. Spot screenshots (CONTEXT.md §D-13 + UI-SPEC §12.6)

8 representative captures attached at `.planning/phases/01-ui-ux-audit-polish/screenshots/`:

- [ ] `01-dashboard-density.png` — `/dashboard` at 1440×900 — KPI tile density
- [ ] `02-equipment-list.png` — `/equipment` table density
- [ ] `03-equipment-filter-empty.png` — `/equipment?type=REEFER&empty=1` filter-empty
- [ ] `04-repair-not-found.png` — `/repair/REP-DOES-NOT-EXIST` not-found with suggestion
- [ ] `05-survey-detail-loading.png` — `/survey/<real-id>?loading=1` detail spinner
- [ ] `06-cleaning-error-disclosure.png` — `/cleaning?error=1` with <details> EXPANDED
- [ ] `07-billing-list.png` — `/billing` table card density
- [ ] `08-tariff-contract-detail.png` — `/tariff/contracts/<real-id>` detail header (verifies no big H1)

## G. Findings + Deviations

(Filled during walkthrough — empty initially.)

| Row | Status | Action | Owner / Plan |
|-----|--------|--------|--------------|
| — | — | — | — |

---

*Tracker generated: 2026-05-18 by gsd-planner / plan 10. Walked by: <agent or human>. Sign-off: pending row-by-row completion (D-14).*
```

Notes on the scaffold:
- Row numbering goes through 48 not 47 because UI-SPEC §12 split the 47 routes across §12.1 (28 + 11 form), §12.2 (10 + 1 edit = 11), §12.4 (3 public) — the actual count is verified by `find src/app -name page.tsx` and any delta is documented.
- The dashboard "filter-empty" is `n/a` per UI-SPEC §12.1 row 1 note.
- Settings form pages have `n/a` for empty/filter-empty (they're forms not lists).
- The form-page section (§12.3) verifies LOADING-ON-SUBMIT and ERROR-ON-SUBMIT only — these are the existing form spinners + alerts, not new state branches.

After writing the file, commit it: `docs(01-10): generate Phase 1 audit tracker scaffold`.
  </action>

  <verify>
    <automated>cd "d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api" && test -f .planning/phases/01-ui-ux-audit-polish/01-AUDIT.md && grep -q "Phase 1 — UI/UX Audit Tracker" .planning/phases/01-ui-ux-audit-polish/01-AUDIT.md && grep -q "Quality Gates" .planning/phases/01-ui-ux-audit-polish/01-AUDIT.md && grep -c "☐" .planning/phases/01-ui-ux-audit-polish/01-AUDIT.md | awk '$1 >= 80 { exit 0 } { print "PENDING COUNT TOO LOW:", $1; exit 1 }' && grep -q "Spot screenshots" .planning/phases/01-ui-ux-audit-polish/01-AUDIT.md && echo "OK — tracker scaffold ready" && echo "--- Actual page count ---" && find src/app -name "page.tsx" | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - `.planning/phases/01-ui-ux-audit-polish/01-AUDIT.md` exists
    - Contains the header (frontmatter + "Phase 1 — UI/UX Audit Tracker")
    - All 6 quality-gate rows present
    - All ≥ 47 page rows present across sections B/C/D/E
    - **W-1 route-count parity gate**: tracker row count equals actual `page.tsx` count. Verify with:
      ```bash
      ACTUAL=$(find src/app -name page.tsx | wc -l)
      TRACKED=$(grep -cE "^| [0-9]+ |" .planning/phases/01-ui-ux-audit-polish/01-AUDIT.md)
      [ "$ACTUAL" -eq "$TRACKED" ] || (echo "MISMATCH: actual=$ACTUAL tracked=$TRACKED" && exit 1)
      ```
      If MISMATCH, add the missing/delete the extra row + document in Section G.
    - Total ☐ Pending cells ≥ 80 (each row has 1-4 cells)
    - 8 spot-screenshot rows present
    - Findings table scaffolded (empty initially)
    - Commit landed: `docs(01-10): generate Phase 1 audit tracker scaffold`
  </acceptance_criteria>

  <done>
Tracker scaffold landed. Ready for Task 2 (quality gates) and Task 3 (browser walkthrough + screenshots).
  </done>
</task>

<task type="auto">
  <name>Task 2: Run all 6 UI-SPEC §14 quality gates autonomously + flip Section A cells</name>

  <read_first>
    - d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api/.planning/phases/01-ui-ux-audit-polish/01-AUDIT.md (just created — read the Section A gates)
    - d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api/.planning/phases/01-ui-ux-audit-polish/01-UI-SPEC.md §14 (the 6 gate definitions)
  </read_first>

  <files>.planning/phases/01-ui-ux-audit-polish/01-AUDIT.md</files>

  <action>
Run each of the 6 quality gates as a shell command. Capture the output. Edit Section A of the tracker accordingly:
- Pass: change `☐ Pending` to `✓ YYYY-MM-DD` (today's date, 2026-05-18)
- Fail: change to `⚠ Deviation: <one-line reason>` and add a corresponding entry to Section G with action

**Gate 1: Density mirrors TOS** (manual — defer to Task 3 human checkpoint; mark as `☐ Pending — checkpoint:human-verify` for now)

**Gate 2: No big page-body H1**
```bash
grep -rln "<h1" src/app/ 2>/dev/null | grep -vE "(src/app/page.tsx|login/page.tsx|forgot-password/page.tsx)" | tee /tmp/h1_violations.txt
[ ! -s /tmp/h1_violations.txt ] && echo "PASS" || cat /tmp/h1_violations.txt
```

**Gate 3: Token discipline** (extract the §6.4 block from gecko_mnr_overlay.css and grep for hex literals within it)
```bash
awk '/6.4   PHASE 1/,/^\/\*/' src/app/gecko_mnr_overlay.css | grep -E "#[0-9a-fA-F]{3,6}\b" | head -5
# Empty output → PASS
```

**Gate 4: State coverage** — count remaining `☐ Pending` cells in Sections B, C, D, E. If 0 → PASS. Otherwise → mark as "pending Task 3 walkthrough" (Task 3 flips them).

**Gate 5: Copy realism (two parts)**
```bash
# Part 1: no placeholder strings in seed
grep -rE "Customer A\b|Container 12345\b|\$[0-9]+\b|Lorem|Sample Co\b" src/ 2>/dev/null | grep -v "node_modules\|/test/\|\.test\." | tee /tmp/placeholder_violations.txt
[ ! -s /tmp/placeholder_violations.txt ] && echo "PART 1 PASS"

# Part 2: BIC validity (plan 02 CI guard from plan 07)
npm test -- equipment.validation 2>&1 | tail -5
```

**Gate 6: Brand string**
```bash
grep -ri "logicon-mnr" src/ 2>/dev/null | grep -v "node_modules" | tee /tmp/logicon_violations.txt
[ ! -s /tmp/logicon_violations.txt ] && echo "PASS"
```

For EACH gate, edit the tracker:
- Replace the `☐ Pending` in the gate row with `✓ 2026-05-18` (or `⚠ Deviation: <output excerpt>` on failure)
- For failures: add a row to Section G (Findings + Deviations) with proposed action

**If ALL automated gates pass:** the tracker shows 5/6 ✓ (Gate 1 stays Pending for Task 3 manual check).

**If any FAIL:** decide whether to fix-in-this-plan (small mechanical issue) or to open a gap-closure recommendation for `/gsd-plan-phase 1 --gaps` (larger structural issue). Document the decision in Section G.

Commit: `docs(01-10): run automated quality gates`.
  </action>

  <verify>
    <automated>cd "d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api" && echo "--- Gate 2 (H1): ---" && grep -rln "<h1" src/app/ | grep -vE "(src/app/page.tsx|login/page.tsx|forgot-password/page.tsx)" | tee /tmp/h1.txt; [ ! -s /tmp/h1.txt ] && echo "Gate 2 PASS" && echo "--- Gate 5 (placeholders): ---" && grep -rE "Customer A\b|Container 12345\b|Lorem\b|Sample Co\b" src/ 2>/dev/null | grep -v "node_modules\|test" | head -5 | tee /tmp/ph.txt; [ ! -s /tmp/ph.txt ] && echo "Gate 5a PASS" && echo "--- Gate 5 (BIC): ---" && npm test -- equipment.validation 2>&1 | tail -3 && echo "--- Gate 6 (brand): ---" && grep -ri "logicon-mnr" src/ 2>/dev/null | tee /tmp/br.txt; [ ! -s /tmp/br.txt ] && echo "Gate 6 PASS" && echo "--- Tracker updated for Section A: ---" && grep "✓ 2026-05-18" .planning/phases/01-ui-ux-audit-polish/01-AUDIT.md | wc -l</automated>
  </verify>

  <acceptance_criteria>
    - Gates 2, 3, 5, 6 run autonomously and either ✓ or ⚠-Deviation in Section A of the tracker
    - Gate 1 (density) noted as "checkpoint:human-verify pending Task 3"
    - Gate 4 (state coverage) noted as "pending row completion in Task 3"
    - For any FAIL: Section G has an entry with proposed action (fix-here OR gap-closure)
    - Commit: `docs(01-10): run automated quality gates`
  </acceptance_criteria>

  <done>
4 of 6 quality gates flipped autonomously. 2 await Task 3 human verification (density side-by-side + row completion). Failures (if any) routed to Section G.
  </done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 3: Browser walkthrough — verify each row in B/C/D/E + capture 8 screenshots + flip cells</name>

  <what-built>
The complete Phase 1 deliverable:
- 5 shared state components (plan 01)
- BIC check-digit util + tests (plan 02)
- 6 shared seed tables (plan 03)
- Per-route copy registry (plan 04)
- PageHeader removed from 20 sites (plan 05)
- Brand strings cleaned (plan 06)
- 12 per-entity seed files with realistic data (plan 07)
- 19 list routes wired (plan 08)
- 11 detail routes wired (plan 09)
- This tracker scaffold (Task 1) + automated quality gates (Task 2)

What you (the human) verify now:
- Each row in Sections B / C / D / E renders correctly when visited at the indicated URL
- The 8 spot screenshots are captured and saved under `.planning/phases/01-ui-ux-audit-polish/screenshots/`
- The density comparison (Gate 1) — open MNR `/dashboard` and TOS `/dashboard/overview` side-by-side at 1440×900 and confirm parity
  </what-built>

  <how-to-verify>
1. **Start the dev server:**
   ```bash
   cd "d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api"
   npm run dev
   ```
   Open http://localhost:3000 (or whichever port).

2. **For Section B (list routes — 25 routes × up to 4 states):**
   For each row, visit the route 4 times with each dev-param:
   - `?empty=1` → confirm EmptyState renders with the correct icon, title, description, primary CTA
   - `?filter-empty=1` → confirm filter-empty variant (or `?type=REEFER&filter-empty=1` etc.)
   - `?loading=1` → confirm TableSkeleton (or KpiTileSkeleton for /dashboard)
   - `?error=1` → confirm ErrorState with "Show details" disclosure (click to expand)

   For each cell:
   - **Renders correctly** → flip `☐` to `✓ 2026-05-18`
   - **Renders but with minor deviation** (e.g. layout slightly off) → `⚠ Deviation: <one-line>`, add to Section G
   - **Crashes / blank / wrong copy** → mark `⚠ DEFECT: <one-line>`, add to Section G with proposed fix

3. **For Section C (detail routes — 11 routes × 2 states):**
   For each row, visit:
   - `<route>/[real-id-from-seed]?loading=1` → confirm DetailSpinner with route-aware label
   - `<route>/DOES-NOT-EXIST` → confirm NotFound with copy + "← Back" CTA. If you typed an ID close to a real one (e.g. `REP-2026-000` when `REP-2026-0001` exists), confirm "Did you mean REP-2026-0001?" appears.

4. **For Section D (form routes — 11 routes × 2 states):**
   For each form, click Submit (with valid data) and confirm:
   - Spinner appears in / next to the submit button while async
   - On simulated failure (currently a simple validation check or `setTimeout` reject), an inline `gecko-alert gecko-alert-error` appears
   - If a form has neither, mark `⚠ Deviation: needs gecko-styled submit spinner/error` and add to Section G

5. **For Section E (public routes — 3 routes):**
   - `/` → confirm `Gecko M&R` hero text, Tan Wei Ming testimonial visible, copyright reads `© 2026 Gecko M&R`
   - `/login` → submit form → spinner shows + error inline on bad creds
   - `/forgot-password` → same pattern

6. **Capture the 8 spot screenshots** at 1440×900 viewport into `.planning/phases/01-ui-ux-audit-polish/screenshots/`:
   - Use the browser's "Save full page" or a screenshot tool
   - Name files per the tracker's Section F (`01-dashboard-density.png`, etc.)
   - Update each Section F checkbox in the tracker to `[x]` after capture

7. **Gate 1 (density side-by-side):**
   - Open `http://localhost:3000/dashboard` in one window
   - Open the TOS sibling repo's `/dashboard/overview` in another window (start its dev server on a different port — see TOS README)
   - Resize both to 1440×900
   - Visually confirm: MNR shows ≥ 4 KPI tiles in the top row + first chart starts above the fold, matching TOS
   - Flip Gate 1 in Section A to `✓ 2026-05-18` or `⚠ Deviation: <density gap>`

8. **Once every row is ✓ / n/a / ⚠**, update the tracker frontmatter `status: in_progress` → `status: complete` and commit: `docs(01-10): complete Phase 1 audit — N rows ✓ / M deviations / 0 pending`.
  </how-to-verify>

  <resume-signal>
Type one of:
- `audit-complete` — every row is ✓ / n/a / deviation-documented; Phase 1 ships
- `gaps-found: <count>` — there are N defects that need fixing; the planner should regenerate Phase 1 plans via `/gsd-plan-phase 1 --gaps` consuming this AUDIT.md
- `paused: <reason>` — partial walkthrough; specify which sections are done
  </resume-signal>
</task>

</tasks>

<verification>
After all 3 tasks:
- `01-AUDIT.md` exists with every row ✓ / n/a / ⚠
- 8 screenshots present under `screenshots/`
- **W-2 screenshot-count gate** (automated D-13 enforcement):
  ```bash
  [ $(ls .planning/phases/01-ui-ux-audit-polish/screenshots/*.png 2>/dev/null | wc -l) -ge 8 ] || (echo "D-13 FAIL: need ≥ 8 screenshots" && exit 1)
  ```
- All 6 quality gates marked ✓ in Section A
- Section G captures any deviations / defects with their disposition
- Tracker frontmatter `status: complete`
- Final commit landed
</verification>

<success_criteria>
1. Tracker scaffolded for all 47 routes × applicable states.
2. All 6 UI-SPEC §14 quality gates run + recorded.
3. Browser walkthrough flips every row (D-14 mandate).
4. 8 D-13 spot screenshots captured.
5. Density side-by-side with TOS at 1440×900 confirmed.
6. Findings routed (fix-in-place OR open `--gaps` for next pass).
7. Phase 1 exit gate cleared: no row in ☐ Pending.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-ux-audit-polish/01-10-SUMMARY.md` documenting:
- Final tracker stats: total rows / ✓ / n/a / ⚠ / pending
- Quality-gate pass count (target 6/6)
- Any deviations and how they were resolved
- Recommendation for next phase: green-light Phase 2 OR run `/gsd-plan-phase 1 --gaps` to close remaining defects
- Hand-off file paths: `.planning/phases/01-ui-ux-audit-polish/01-AUDIT.md` + `screenshots/`
</output>
