---
phase: 01-ui-ux-audit-polish
plan: 06
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/page.tsx
  - src/app/login/page.tsx
  - src/app/forgot-password/page.tsx
  - src/app/settings/integrations/page.tsx
requirements: [UI-03]
autonomous: true

must_haves:
  truths:
    - "Zero occurrences of 'logicon-mnr' anywhere in src/ (case-insensitive, excluding .gitignore and comments-of-record)"
    - "Zero occurrences of bare 'logicon' as a brand identifier (some URL/domain-style 'logicon' may persist if they are legitimate technical identifiers, but every brand-text 'logicon' becomes 'Gecko')"
    - "Landing page footer copyright reads '© 2026 Gecko M&R' (D-16)"
    - "Login left-panel testimonial replaced with the UI-SPEC §10.4 stable Tan Wei Ming quote (D-17 picked one and frozen)"
    - "<title> template in app/layout.tsx already correct (no edit) — confirmed in PATTERNS.md §13"
    - "Login + forgot-password submit chrome uses gecko-btn / gecko-spinner / gecko-alert-error classes per UI-SPEC §7.4 (W-7)"
    - "PRE-FLIGHT GATE prevents race with plan 05: zero PageHeader imports remain in public pages before Task 1 begins (B-3)"
  artifacts:
    - path: "src/app/page.tsx"
      provides: "Brand-cleaned landing + testimonial replacement"
      contains: "Gecko M&R"
      not_contains: "logicon-mnr"
    - path: "src/app/login/page.tsx"
      provides: "Brand-cleaned login left-panel + testimonial"
      contains: "Tan Wei Ming"
      not_contains: "logicon-mnr"
    - path: "src/app/forgot-password/page.tsx"
      provides: "Brand-cleaned forgot-password"
      not_contains: "logicon-mnr"
    - path: "src/app/settings/integrations/page.tsx"
      provides: "Brand-cleaned integrations strings (URL fields may keep 'logicon' if technical)"
      not_contains: "logicon-mnr"
  key_links:
    - from: "UI-SPEC §14 'Brand string' quality gate"
      to: "grep -ri 'logicon-mnr' src/"
      via: "must return 0 hits"
      pattern: "logicon-mnr"
---

<objective>
Replace residual `logicon-mnr` / `logicon` brand strings with `Gecko M&R` / `Gecko` in the 4 files identified by PATTERNS.md §13. Replace the landing-page testimonial with the UI-SPEC §10.4 frozen quote (per D-17). Make the UI-SPEC §14 "Brand string" quality gate pass: `grep -ri 'logicon-mnr' src/` returns ZERO.

Purpose: D-16 (brand string lockdown) is one of the user-locked decisions and one of the 6 UI-SPEC §14 quality gates. Auditor (plan 10) will run the grep gate as a hard block. The 4 files are narrow and well-identified — this plan ships in Wave 1 independently of every other plan (no file overlap with plans 01-05 or 07-10).

Output: 4 file edits, all string-literal substitutions. Zero structural changes.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-ui-ux-audit-polish/01-CONTEXT.md
@.planning/phases/01-ui-ux-audit-polish/01-UI-SPEC.md
@.planning/phases/01-ui-ux-audit-polish/01-PATTERNS.md

<interfaces>
<!-- From PATTERNS.md §13 — the exact replacement sites + replacements -->

| File | Line(s) | Current | Replacement |
|------|---------|---------|-------------|
| src/app/page.tsx | ~119 | `logicon-mnr` (hero nav)          | `Gecko M&R` |
| src/app/page.tsx | ~574 | `logicon-mnr` (footer brand)     | `Gecko M&R` |
| src/app/page.tsx | ~641 | `&copy; 2024 logicon-mnr`        | `© 2026 Gecko M&R` |
| src/app/login/page.tsx | ~73 | `logicon-mnr` (left-panel hero) | `Gecko M&R` |
| src/app/forgot-password/page.tsx | (similar) | `logicon-mnr` | `Gecko M&R` |
| src/app/settings/integrations/page.tsx | (line per grep) | brand-text `logicon` strings | `Gecko` |

<!-- The frozen testimonial quote from UI-SPEC §10.4 (D-17) -->

UI-SPEC §10.4 option (chosen and frozen):
> "Gecko gave our depot crew the standards alignment we'd been chasing on
>  spreadsheets for years."
> — Tan Wei Ming, Yard Operations Manager, PSA-affiliated ICD, Singapore

REPLACE the current testimonial (per CONTEXT.md):
> "Somchai Prasert, Operations Manager, CMA CGM Thailand"

<!-- Login form Loading + Error already present per PATTERNS.md §13 — no functional change needed -->
<!-- src/app/layout.tsx already says "Gecko M&R" (PATTERNS.md line 99) — DO NOT edit it -->
</interfaces>
</context>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| string literals → public-page render | Public pages don't fetch data — pure marketing/auth surfaces. Editing string literals has no security implication. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-06-01 | Tampering (regression) | Edits to public-page JSX | mitigate | `npm run build` must pass; `grep` gate confirms no logicon-mnr remains; manual smoke at `/`, `/login`, `/forgot-password` during plan 10 audit. |
| T-06-02 | Information Disclosure | Brand-string leakage in production | mitigate | This plan IS the mitigation — locks down the legacy brand so the production site doesn't ship the old company name. |
| T-06-03 | Spoofing (false testimonial) | New testimonial attribution | accept | UI-SPEC §10.4 D-17 chose this attribution explicitly as a realistic SE-Asia ICD operator quote; no real-person impersonation risk because the quote is positioned as a marketing-stock testimonial pattern. |
</threat_model>

<tasks>

<task type="auto">
  <name>Task 1: Discover exact occurrences + replace logicon strings + testimonial in 4 files</name>

  <read_first>
    - d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api/src/app/page.tsx (READ FULL — find every logicon occurrence + the current testimonial block)
    - d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api/src/app/login/page.tsx (READ FULL)
    - d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api/src/app/forgot-password/page.tsx (READ FULL)
    - d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api/src/app/settings/integrations/page.tsx (READ FULL)
    - d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api/src/app/layout.tsx (READ ONLY lines 1-30 to confirm title metadata already says "Gecko M&R" — do NOT edit this file)
    - d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api/.planning/phases/01-ui-ux-audit-polish/01-UI-SPEC.md §10.4 (lines 797-810 — the frozen testimonial)
    - d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api/.planning/phases/01-ui-ux-audit-polish/01-CONTEXT.md D-16, D-17

    **PRE-FLIGHT GATE (B-3 race-prevention with plan 05):** Before any edit, run:
    ```bash
    grep -l "PageHeader" src/app/page.tsx src/app/login/page.tsx src/app/forgot-password/page.tsx 2>/dev/null
    ```
    This MUST return ZERO files. If it returns any file, plan 05 has not yet finished cleaning the
    public-page PageHeader references, and this task MUST fail-fast with the message:
    `"WAIT for plan 05 to complete PageHeader cleanup on public pages before plan 06 proceeds."`
    Executor SHOULD sleep + retry the gate in 30-60 second intervals until it returns empty
    (dynamic dependency on plan 05 completing its public-page touches).
    Rationale: plan 05 and plan 06 both modify these 3 files in Wave 1; ordering is needed to
    avoid merge conflicts. Keeping plan 06 in Wave 1 preserves parallelism; the gate enforces
    correctness.
  </read_first>


  <files>src/app/page.tsx, src/app/login/page.tsx, src/app/forgot-password/page.tsx, src/app/settings/integrations/page.tsx</files>

  <action>
**Step A: Run authoritative grep** to find every `logicon` occurrence:
```
grep -rn -i "logicon" src/ | grep -v node_modules
```
Save the output. This IS the complete list to address — PATTERNS.md §13 estimates 4 files, but the grep is authoritative.

**Step B: For each occurrence, apply the replacement.**

**Replacement rules:**
1. `logicon-mnr` → `Gecko M&R` (always — D-16 brand identifier)
2. `Logicon-MNR` / `LOGICON-MNR` etc. → `Gecko M&R` (case-aware)
3. Bare `logicon` as a brand-text identifier (e.g. hero text, footer brand, copyright) → `Gecko`
4. Bare `logicon` as a TECHNICAL identifier (URLs like `https://api.logicon.example`, env var names, CSS class names) — INSPECT case-by-case. If it's a legitimate technical handle that has no security/branding impact and replacing would break something, leave it and note in the SUMMARY. If it's brand-text rendered in the UI, replace.
5. `&copy; 2024 logicon-mnr` → `© 2026 Gecko M&R` (year + symbol upgrade per PATTERNS.md §13 example)
6. `&copy;` → `©` (use the Unicode character; cleaner render)
7. Year `2024` → `2026` in copyright contexts only (project date = 2026-05-18; today is correct)

**Step C: Replace the landing-page testimonial in `src/app/page.tsx`.**

Find the current testimonial block (it currently contains `Somchai Prasert, Operations Manager, CMA CGM Thailand`). Replace BOTH the quote text AND the attribution.

Replace with the UI-SPEC §10.4 frozen text (use straight ASCII quotes in the source — JSX renders them fine):
- Quote: `Gecko gave our depot crew the standards alignment we'd been chasing on spreadsheets for years.`
- Attribution: `Tan Wei Ming, Yard Operations Manager, PSA-affiliated ICD, Singapore`

Notes:
- The current testimonial JSX shape stays — only the strings change. If the current uses `<blockquote>` + `<cite>` keep that structure.
- If the current uses an avatar/photo placeholder, leave it (a real photo arrives in a marketing phase, not Phase 1).

**Step D: Verify the title metadata.**

Open `src/app/layout.tsx` and confirm `Gecko M&R` is already in the title template (PATTERNS.md line 99 verifies this). DO NOT edit `layout.tsx` — it's not in this plan's files_modified.

If the grep from Step A returned `layout.tsx` (unexpected), STOP and report — the planning input is wrong.

**Step E: For settings/integrations brand strings**, inspect carefully:
- Strings like `<label>Logicon API URL</label>` or hero/heading text → replace with `Gecko`
- Strings like `https://api.logicon.com/v1` or `LOGICON_API_KEY` env vars → leave alone unless obviously brand-text. If unclear, leave and document in the SUMMARY for the user to review.
- The settings/integrations page is a stub-list of future integrations (no integrations live in v1 per CONTEXT.md / PROJECT.md); brand-text there is purely visual.

**Iteration order:** Process the 4 files in this order: page.tsx → login/page.tsx → forgot-password/page.tsx → settings/integrations/page.tsx. For each:
1. Re-Read the file (it may have changed since Step A).
2. Apply each Edit with the precise old_string from the file (long enough to be unique — include 1-2 surrounding tokens).
3. Re-Verify with grep.
  </action>

  <verify>
    <automated>cd "d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api" && echo "--- 'logicon-mnr' must be 0 (CASE-INSENSITIVE) outside node_modules: ---" && (grep -ri "logicon-mnr" src/ 2>/dev/null | tee /tmp/logicon_mnr_hits.txt) && [ ! -s /tmp/logicon_mnr_hits.txt ] && echo "--- 'logicon' brand text in src/ (review remaining hits — should be technical-only): ---" && grep -ri "logicon" src/ 2>/dev/null | head -20 && echo "--- New testimonial present: ---" && grep -q "Tan Wei Ming" src/app/page.tsx && echo "--- Old testimonial removed: ---" && ! grep -q "Somchai Prasert" src/app/page.tsx && echo "--- Gecko M&R present in landing/login/forgot: ---" && grep -q "Gecko M&R" src/app/page.tsx && grep -q "Gecko" src/app/login/page.tsx && echo "--- TS clean: ---" && npx tsc --noEmit 2>&1 | tail -5 && echo "--- Build clean: ---" && npm run build 2>&1 | tail -10 && echo "OK"</automated>
  </verify>

  <acceptance_criteria>
    - PRE-FLIGHT GATE (B-3) passed before any edit: `grep -l "PageHeader" src/app/page.tsx src/app/login/page.tsx src/app/forgot-password/page.tsx` returned ZERO files (plan 05 finished cleaning public pages first)
    - `grep -ri "logicon-mnr" src/` returns ZERO hits (this is the UI-SPEC §14 quality gate)
    - `grep -q "Tan Wei Ming" src/app/page.tsx` returns 0 (new testimonial present)
    - `! grep -q "Somchai Prasert" src/app/page.tsx` (old testimonial removed)
    - `grep -q "PSA-affiliated ICD, Singapore" src/app/page.tsx` returns 0 (new attribution present)
    - `grep -q "Gecko M&R" src/app/page.tsx` returns 0
    - `grep -q "Gecko" src/app/login/page.tsx` returns 0
    - `grep -q "Gecko" src/app/forgot-password/page.tsx` returns 0
    - Copyright line in `src/app/page.tsx` reads `© 2026 Gecko M&R` (or equivalent — year is current, symbol is Unicode ©)
    - `src/app/layout.tsx` was NOT modified (the title template was already correct)
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
    - Any remaining bare `logicon` occurrences in `src/` are documented in the SUMMARY as legitimate technical identifiers (URL fragments, etc.)
  </acceptance_criteria>

  <done>
4 files edited. UI-SPEC §14 brand-string grep gate PASSES (`logicon-mnr` count = 0). Landing testimonial replaced with the frozen Tan Wei Ming quote per D-17. Build clean.
  </done>
</task>

<task type="auto">
  <name>Task 2: Swap login + forgot-password submit chrome to gecko classes per UI-SPEC §7.4 (W-7)</name>

  <read_first>
    - d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api/src/app/login/page.tsx (READ FULL — locate current submit-button loading state + auth-failure error rendering)
    - d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api/src/app/forgot-password/page.tsx (READ FULL — same as above)
    - d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api/src/app/gecko_design_system_components.css (READ — confirm the .gecko-btn disabled, .gecko-spinner, and .gecko-alert.gecko-alert-error classes already exist)
    - d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api/.planning/phases/01-ui-ux-audit-polish/01-UI-SPEC.md §7.4 (lines 577-586 — the narrower public-page bar)
  </read_first>

  <files>src/app/login/page.tsx, src/app/forgot-password/page.tsx</files>

  <action>
Per UI-SPEC §7.4, public pages need:
1. Loading state on form submit → spinner inside the submit button (gecko-btn disabled + inline gecko-spinner).
2. Error state on auth/submit failure → inline `<div className="gecko-alert gecko-alert-error">`.

**Step A: Inspect existing submit chrome.**
Both pages already have submit handlers. They likely use Tailwind / lucide chrome (e.g. `<Loader2 className="animate-spin" />` and `<div className="bg-red-50 ...">`). The task is to swap the CHROME to the gecko design-system classes — NOT to add new backend wiring (no real auth call lands in Phase 1).

**Step B: Replace the loading-state chrome on the submit button.**

Find the submit button. Replace the spinner-rendering pattern with:
```tsx
<button
  type="submit"
  className="gecko-btn gecko-btn-primary"
  disabled={isSubmitting}
>
  {isSubmitting && <span className="gecko-spinner" aria-hidden="true" style={{ marginRight: 8 }} />}
  {isSubmitting ? 'Signing in…' : 'Sign in'}
</button>
```

For forgot-password use the appropriate labels (`'Sending reset link…'` / `'Send reset link'`).

The `disabled={isSubmitting}` attribute automatically triggers the gecko-btn disabled state already in the design-system CSS — no extra class needed.

**Step C: Replace the auth-failure error chrome.**

Find the error-rendering block (typically conditional on a state variable like `authError` or `errorMessage`). Replace with:
```tsx
{authError && (
  <div
    className="gecko-alert gecko-alert-error"
    role="alert"
    style={{ marginTop: 16 }}
  >
    {authError}
  </div>
)}
```

The `role="alert"` attribute is added for screen-reader compatibility — this is a no-cost a11y win that the existing chrome likely lacked.

**Step D: Leave everything else untouched.**
- DO NOT modify the actual submit handler logic (the Phase-1 bar is "chrome only").
- DO NOT add new state variables — reuse whatever the current page tracks.
- DO NOT modify the page layout / heading / testimonial (Task 1 owns testimonial / brand-string edits).
- DO NOT touch any code outside the submit button + error-rendering block.

**Iteration order:** login/page.tsx first, forgot-password/page.tsx second. Re-Read each file before editing (Task 1 may have already touched it).

**Race-prevention note:** Task 1 also touches these 2 files (logicon string replacement). Run Task 2 AFTER Task 1 completes on the same file. Sequential within this plan — both tasks in plan 06 are auto type so the executor runs them serially.
  </action>

  <verify>
    <automated>cd "d:/SHARMA/PROJECT/gecko/web.mnr.gecko-api" && grep -q "gecko-btn gecko-btn-primary" src/app/login/page.tsx && grep -q "gecko-spinner" src/app/login/page.tsx && grep -q "gecko-alert gecko-alert-error" src/app/login/page.tsx && grep -q "gecko-btn gecko-btn-primary" src/app/forgot-password/page.tsx && grep -q "gecko-spinner" src/app/forgot-password/page.tsx && grep -q "gecko-alert gecko-alert-error" src/app/forgot-password/page.tsx && npx tsc --noEmit 2>&1 | tail -5 && npm run build 2>&1 | tail -10 && echo "OK"</automated>
  </verify>

  <acceptance_criteria>
    - `src/app/login/page.tsx` submit button uses `className="gecko-btn gecko-btn-primary"` (grep)
    - `src/app/login/page.tsx` loading state uses `gecko-spinner` inline span (grep)
    - `src/app/login/page.tsx` auth-error block uses `gecko-alert gecko-alert-error` (grep)
    - `src/app/forgot-password/page.tsx` submit button uses `gecko-btn gecko-btn-primary` (grep)
    - `src/app/forgot-password/page.tsx` loading state uses `gecko-spinner` (grep)
    - `src/app/forgot-password/page.tsx` error block uses `gecko-alert gecko-alert-error` (grep)
    - `! grep -q "animate-spin\|Loader2\|bg-red-" src/app/login/page.tsx` (legacy Tailwind / lucide chrome removed from submit area — grep should not find these tokens; if the page uses them elsewhere outside the submit chrome, document in SUMMARY)
    - `! grep -q "animate-spin\|Loader2\|bg-red-" src/app/forgot-password/page.tsx`
    - `npx tsc --noEmit` exits 0
    - `npm run build` exits 0
    - Plan 10 audit row 47 (`/login` Loading + Error) and row 48 (`/forgot-password` Loading + Error) will now flip ✓ when manually verified in browser
  </acceptance_criteria>

  <done>
Login + forgot-password submit chrome now uses gecko-btn / gecko-spinner / gecko-alert-error classes per UI-SPEC §7.4. Plan 10 audit rows for these 2 public pages can pass. No backend wiring added.
  </done>
</task>

</tasks>

<verification>
- `grep -ri "logicon-mnr" src/` returns 0 (HARD GATE)
- New testimonial present, old testimonial removed
- Copyright updated to 2026 + © symbol
- `npm run build` exits 0
- Manual smoke: `npm run dev`, visit `/`, `/login`, `/forgot-password` — no `logicon-mnr` text visible anywhere
</verification>

<success_criteria>
1. Zero `logicon-mnr` strings remain in `src/`.
2. Landing testimonial replaced verbatim per UI-SPEC §10.4 D-17 choice (Tan Wei Ming, PSA-affiliated ICD, Singapore).
3. Copyright line current-year + Unicode ©.
4. Login + forgot-password brand strings clean.
5. Settings/integrations brand-text strings clean (technical URLs preserved if any, documented).
6. `src/app/layout.tsx` untouched (already correct per PATTERNS.md verification).
7. UI-SPEC §14 "Brand string" quality gate PASSES.
8. Build clean.
</success_criteria>

<output>
After completion, create `.planning/phases/01-ui-ux-audit-polish/01-06-SUMMARY.md` documenting:
- Authoritative count of replacements per file (with line numbers from the grep)
- The frozen testimonial choice (D-17 — Tan Wei Ming quote)
- Any remaining bare `logicon` occurrences flagged as legitimate technical identifiers, with rationale
- Confirmation that `src/app/layout.tsx` was untouched (title metadata already correct)
</output>
