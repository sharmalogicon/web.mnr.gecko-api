---
phase: 07-three-tier-tariff
type: context
generated: 2026-05-19
status: locked
milestone: v1.1-A
upstream:
  - .planning/MILESTONES.md (v1.0 close-out 2026-05-19)
  - src/data/seed/_shared/cedex.ts (Phase 4 — reused for charge codes)
  - src/data/seed/_shared/customers.ts (10 liners)
  - src/data/seed/_shared/depots.ts (7 depots)
---

# Phase 7 — 3-Tier Tariff Restructure — CONTEXT

First phase of milestone v1.1-A. Restructures the tariff system from the v1.0 flat `rate-cards + customer-rates + contracts` model into three clean lanes:

- **Standard tariff** (revenue baseline, per-depot)
- **Liner tariff** (revenue per shipping line — supersedes customer-rates + contracts)
- **Vendor tariff** (cost-side: what suppliers charge us when we outsource)

All three share the same `<TariffCard>` page pattern modeled on the TOS Customer Rate Profile screen (user reference).

## Decisions (locked, with user 2026-05-19)

| ID | Decision | Why |
|----|----------|-----|
| D-01 | **Standard tariff per-depot.** 7 cards (LCB / LKR / PKN / PKW / PGU / JUR / PPP). Fallback chain: Liner row → matching depot's Standard row → "no rate" warning. | User answer Q1. Different cost bases per depot. |
| D-02 | **Liner tariff per shipping line.** 10 cards (Maersk / CMA CGM / MSC / ONE / Hapag-Lloyd / Evergreen / COSCO / Yang Ming / HMM / ZIM). Header: agent code, contact, sales person, effective + expiry, quotation no, approval audit. Line items: CEDEX-coded charges + explicit billing unit + THB rate. | User answer Q2. |
| D-03 | **Charge codes derive from CEDEX (Phase 4 seed).** Two namespaces: (a) CEDEX-derived `<component>-<repair>` combos (LIN-PAT, FNX-STR, GAS-RPL, VLV-RPL, CCS-WLD, etc.) — ~25 codes; (b) `SVC-*` prefix for non-repair services (SVC-SURVEY-DRY, SVC-PTI, SVC-WASH-FOOD, SVC-STG-NORM, SVC-GATE-IN, SVC-EMERG, etc.) — ~15 codes. **Total ~40 charge codes.** | User answer Q3 ("use CEDEX codes boss"). Operator continuity from Phase 4. |
| D-04 | **Billing units explicit per row.** Enum: `CONT \| TEU \| HOUR \| DAY \| JOB \| KG \| M`. UI displays "850 THB / CONT" or "350 THB / DAY" so the unit is unambiguous. | User emphasized UNIT specifications in Q2. |
| D-05 | **THB only.** No currency field; all rates are THB. | User answer Q3 (carry forward from Phase 4). |
| D-06 | **Print button stays, no-op for now.** Renders on card footer. Onclick triggers `window.print()` against the page as-is (same approach as PTI cert from Phase 6). No PDF library, no print-stylesheet customization beyond hiding the action row. | User answer Q4. |
| D-07 | **Clone (Liner cards only).** Copies all rows, resets status to DRAFT, clears `approvedBy`/`approvedOn`, generates new quotation number ON SAVE (not on clone — canceling a clone doesn't burn numbers). Lands user on `/tariff/liner/[newAgentCode]/edit`. | User answer Q5. |
| D-08 | **Liner = Agent.** No separate customer/forwarder concept (TOS has all three; MNR Phase A simplified to just Agent = Liner = billing target). `billedTo` field pinned to `'AGENT'` on every row. | User direction from screenshot annotation. |
| D-09 | **Universal `<TariffCardPage>` + `<ChargesTable>` + `<ChargeRowEditor>` (modal) + `<TariffCardFooter>`.** Built once with react-hook-form + Zod, reused across all 3 lanes. Modal uses existing `@radix-ui/react-dialog`. | Same shape as the TOS reference; reduces 3 lanes × N pages of duplication. |
| D-10 | **Approval state machine.** `DRAFT → APPROVED → EXPIRED` (auto on expiry date). Only `APPROVED` rows apply at quote time. Approve/Un Approve buttons on footer match the TOS pattern. | TOS screenshot reference. |
| D-11 | **Quotation number scheme: `QU-YYYY-NNNNN`.** 5-digit serial per year, generated by repo helper `nextQuotationNo(year?)` on save only. Standard cards don't have quotation numbers (they're the baseline); only Liner cards do. Vendor cards get a similar `VQ-YYYY-NNNNN` scheme. | Simpler than TOS's QUSCT prefix scheme. |
| D-12 | **Simulator upgraded to show Revenue / Cost / Margin.** Inputs: Agent + Depot + Charge code + Size + Cargo category + optional Vendor. Outputs: Revenue (liner→standard fallback with path shown) + Cost (vendor) + Margin. | Phase 7 — the big practical win. |
| D-13 | **Phase 4 CEDEX seed unchanged.** Phase 7 reads from `src/data/seed/_shared/cedex.ts`; does not modify. Existing CEDEX consumers (`/repair/new` form) keep working. | Additive only. |
| D-14 | **Existing rate-cards / customer-rates / contracts seeds retired.** New `standard-tariff.ts`, `liner-tariff.ts`, `vendor-tariff.ts` replace them. Repos in `src/lib/repos/index.ts` swap to the new ones. Pages using `/rate-cards`, `/customer-rates`, `/contracts` redirect to the new routes. | Clean migration. |
| D-15 | **Surcharges + History keep their existing shape.** History now logs edits across all 3 lanes (Standard / Liner / Vendor). Surcharges are unchanged — they apply on top of any tariff. | Minimal disruption to working features. |
| D-16 | **Phase 7 ships when:** (a) 5 catalog seeds exist (charge-codes, order-types, movement-codes, cargo-categories, vendors), (b) 3 tariff card seeds exist with realistic data, (c) 3 repos exposed via wiring file with create/update/clone/approve/unapprove/nextQuotationNo, (d) all 12 new routes render the tariff-card pattern, (e) legacy URL redirects in place, (f) simulator upgraded, (g) tsc + tests clean, (h) BIC + IICL-6 CI guards remain green. | Goal-backward exit criteria. |

## Charge code namespace (full list per D-03)

### CEDEX-derived (component + repair, ~25 codes)

Derived from `cedexComponents` × `cedexRepairs` in Phase 4 seed:

`LIN-PAT`, `LIN-COA`, `FNX-STR`, `FNX-RPL`, `SDR-STR`, `SDR-RPL`, `DRH-STR`, `DRH-RPL`, `GAS-RPL`, `LCK-RPL`, `CAM-RPL`, `CCS-WLD`, `CCS-STR`, `FLR-RPL`, `FLR-PAT`, `SHL-WLD`, `SHL-COA`, `SHL-STR`, `VLV-RPL`, `MAN-CLN`, `MAN-RPL`, `CMP-REP`, `EVA-CLN`, `EVA-RPL`, `FAN-RPL`, `SNS-RPL`

### Service-prefix (~15 codes)

`SVC-SURVEY-DRY`, `SVC-SURVEY-TANK`, `SVC-SURVEY-REEF`, `SVC-PTI`, `SVC-WASH-STD`, `SVC-WASH-FOOD`, `SVC-WASH-CHEM`, `SVC-STG-NORM`, `SVC-STG-REEF`, `SVC-STG-DG`, `SVC-GATE-IN`, `SVC-GATE-OUT`, `SVC-EMERG`, `SVC-LABOR-HR`, `SVC-PLUG-IN`

## Out of scope

- Multi-currency (THB only per D-05)
- Real PDF generation (browser print only per D-06)
- TOS-style 3-party (Customer/Forwarder/Agent) header — MNR simplified to Agent only per D-08
- Role-based auth on Approve (carried-forward Phase 4 D-T4-Auth residual; no auth context in v1)
- Real-time tariff sync to TOS or any external system
- Bulk-import tariff (manual editing only in v1)

## Residuals carried forward

- Phase 1 Task 3 walkthrough
- Phase 3 form-walk visual verification
- Phase 4 CEDEX + IICL-6 stub-data
- Phase 4 no-auth-on-Approve
- Phase 5 checklist stub phrasing
- Phase 6 cert direct-URL-only
- NEW Phase 7 residual: tariff-form visual-walk (same human-verify pattern)

---

*Locked: 2026-05-19 by Claude after user lane decisions on per-depot Standard, CEDEX-derived charge codes, no-op Print, Clone-resets-to-DRAFT, THB only.*
