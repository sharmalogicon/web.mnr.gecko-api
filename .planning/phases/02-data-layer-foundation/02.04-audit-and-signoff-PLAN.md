---
phase: 02-data-layer-foundation
plan: 04
type: execute
wave: 3
depends_on: [01, 02, 03]
files_created:
  - .planning/phases/02-data-layer-foundation/02-AUDIT.md
  - .planning/phases/02-data-layer-foundation/02-SUMMARY.md
requirements: [DATA-01, DATA-02]
autonomous: true
---

# Plan 02.04 — Phase 2 audit + sign-off

## Goal

Run all 02-PATTERNS.md verification gates, record results in `02-AUDIT.md`, write `02-SUMMARY.md`, and advance STATE.md to Phase 3.

## Gates (all autonomous; no human-verify checkpoint this phase)

| # | Gate | Command | Pass |
|---|------|---------|------|
| 1 | Types directory coverage | `ls src/lib/types/*.ts \| wc -l` + tariff | 12 + 5 = 17 |
| 2 | Repos directory coverage | `ls src/lib/repos/*.ts \| wc -l` + tariff | 12 + 5 = 17 |
| 3 | HTTP stub present | `test -f src/lib/repos/http/equipment.ts` | exists |
| 4 | No page imports from `@/data/seed/<entity>` | grep test | 0 hits |
| 5 | Single-swap audit | `grep -rEn "new InMemory[A-Z]" src/` | hits only in `src/lib/repos/index.ts` |
| 6 | Page-side repo usage | `grep -rln 'from "@/lib/repos"' src/app/` | ≥ 25 hits |
| 7 | tsc clean | `npx tsc --noEmit` | exit 0 |
| 8 | Test suite | `npm test` | 17/17 |

## Success criterion recheck (REQUIREMENTS.md)

- **DATA-01 #1**: "every list / detail page renders from a single shared typed data source" → ✓ Gate 4 + 6
- **DATA-01 #2**: developer can swap repo in a single wiring file → ✓ Gate 5 (single swap point) + Gate 3 (HTTP stub demonstrates the signature)
- **DATA-01 #3**: every entity declared as exported TS type used by reads + writes → ✓ Gate 1 + page-side type imports
- **DATA-02**: stub fetch-based HTTP repository in single wiring file → ✓ Gate 3 (stub exists) + 02-SUMMARY documents the swap recipe

## Out-of-band check

- The future REST team's API shape should match the `EntityRepo` interface in `src/lib/repos/<entity>.ts`. Document this in 02-SUMMARY as a hand-off note.
- Phase 1 Task 3 deferred residual unchanged.

## 02-SUMMARY.md content

- Files created + modified counts
- Gate pass count (8/8 expected)
- HTTP swap recipe (step-by-step)
- Hand-off note to REST team about the EntityRepo signature
- Green-light for Phase 3 (Equipment Master & ISO 6346)

## Acceptance

- 02-AUDIT.md written with all 8 gates
- 02-SUMMARY.md written
- STATE.md advances `current_position` to Phase 3 (planning)
- ROADMAP.md progress table flips Phase 2 to "Shipped"
- MILESTONES.md Phase 2 entry added
- Atomic commit: `docs(02-04): close Phase 2 — repository layer shipped`
